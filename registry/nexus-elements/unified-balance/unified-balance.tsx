import * as React from "react";

interface UnifiedBalanceProps {
  propName: "type";
}

const UnifiedBalance: React.FC<UnifiedBalanceProps> = ({ propName }) => {
  return <div></div>;
};

export default UnifiedBalance;
